'use strict';

// const homePage = $('#app');

// $.get('', (res) => {
//     console.log(res);
    // for (const berry of res.results) {
//     //     berries.append(`<li>${berry.name}</li>`);
//   }
// );